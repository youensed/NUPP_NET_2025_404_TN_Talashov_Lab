@model IEnumerable<PetStore.MVC.Models.DogViewModel>

@{
    ViewData["Title"] = "Собаки";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>@ViewData["Title"]</h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Додати собаку
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Ім'я</th>
                        <th>Вік</th>
                        <th>Порода</th>
                        <th>Дресирований</th>
                        <th>Дії</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var dog in Model)
                    {
                        <tr>
                            <td>@dog.Name</td>
                            <td>@dog.Age років</td>
                            <td>@dog.Breed</td>
                            <td>
                                @if (dog.IsTrained)
                                {
                                    <span class="badge bg-success">Так</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Ні</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-action="Details" asp-route-id="@dog.Id" class="btn btn-sm btn-info">
                                        <i class="bi bi-eye"></i> Деталі
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@dog.Id" class="btn btn-sm btn-warning">
                                        <i class="bi bi-pencil"></i> Редагувати
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@dog.Id" class="btn btn-sm btn-danger">
                                        <i class="bi bi-trash"></i> Видалити
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <i class="bi bi-info-circle"></i> Немає собак для відображення. <a asp-action="Create">Додайте першого!</a>
        </div>
    }
</div>

